-- Default programs (was: modules/keywords.conf $variables)

return {
    terminal    = "ghostty",
    fileManager = "nemo",
    menu        = 'rofi -combi-modi window,drun,ssh -theme solarized -font "hack 14" -show combi -icon-theme "Papirus" -show-icons',
}
